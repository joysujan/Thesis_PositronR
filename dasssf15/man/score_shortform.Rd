% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_shortform.R
\name{score_shortform}
\alias{score_shortform}
\title{Score DASS-15 and optionally approximate DASS-21}
\usage{
score_shortform(
  df,
  cut = c("Youden", "Sens>=0.80"),
  legacy = TRUE,
  legacy_method = c("equiperc", "linear")
)
}
\arguments{
\item{df}{Data frame with the 15 DASS short-form item columns coded 0..3, names matching the model.}

\item{cut}{Character. Operating point name; one of 'Youden' or 'Sens>=0.80'. Default 'Youden'.}

\item{legacy}{Logical. If TRUE, return approximate DASS-21 total/category. Default TRUE.}

\item{legacy_method}{'equiperc' (default) or 'linear' for legacy mapping.}
}
\value{
A data.frame with columns: theta, se, theta_cal, elevated, and optional legacy fields.
}
\description{
Compute IRT EAP theta and SE from the 15-item short form, calibrate to the SEM latent,
apply the published ROC cut for an elevated flag, and optionally return an approximate
DASS-21 total and category via pre-trained links (equipercentile or linear).
}
\examples{
ex <- readr::read_csv(system.file('examples','example_input_20rows.csv', package='dasssf15'), show_col_types = FALSE)
out <- score_shortform(ex, cut='Youden', legacy=TRUE, legacy_method='equiperc')
head(out)
}
